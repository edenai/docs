pytest
pytest-cov
pytest-timeout
pytest-xdist
requests
numpy
openai
langchain~=1.2
langchain-openai~=1.1
langchain-community~=0.4
langchain-text-splitters
langgraph~=1.0
chromadb
pydantic
python-dotenv
aiohttp
PyPDF2
Pillow
prometheus_client
datadog

# integration guides (v3/integrations/)
any-llm-sdk[edenai]
# An unrelated atomic-agents 0.1 also exists on PyPI, and the resolver falls
# back to it when the real framework's pins conflict, so require v2.
atomic-agents>=2
edenai-haystack
pydantic-ai
